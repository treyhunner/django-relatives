language: python

python:
  - 3.6
  - 3.7
  - 3.8
  - 3.9

env:
  - DJANGO=Django==2.2
  - DJANGO=Django==3.0
  - DJANGO=Django==3.1
  - DJANGO=Django==3.2

install:
  - pip install $DJANGO
  - pip install coverage coveralls

script: coverage run setup.py test

after_success: coveralls
